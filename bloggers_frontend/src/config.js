const config = {
    development: {
        apiUrl: "http://localhost:4000",
    },

    production: {
        apiUrl: "https://bloggers-z-api.herokuapp.com/",
    },
    test: {
        apiUrl: "",
    },
};

export default config;
